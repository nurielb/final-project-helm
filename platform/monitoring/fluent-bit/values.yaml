kind: DaemonSet

config:
  outputs: |
    [OUTPUT]
        Name stdout
        Match *
        Format json_lines

    [OUTPUT]
        Name loki
        Match *
        Host loki-gateway.monitoring.svc.cluster.local
        Port 80
        Labels job=fluent-bit
        Auto_Kubernetes_Labels On