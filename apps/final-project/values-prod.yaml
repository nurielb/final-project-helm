deployment:
  metadata:
    name: final-project
  replicas: 3
container:
  port: 5000
  image:
    name: nurielbenjamin/final-project-backend
    tag: "prod-9d5e6a1"
service:
  metadata:
    name: final-project-service
  port: 80
  targetPort: 5000
  type: ClusterIP
imagePullSecrets:
  - name: regcred
